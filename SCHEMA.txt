schema: 1 | 2
client_time: iso_8601
time: iso_8601

proxy_context: {
  via_reverse_proxy: boolean
  is_request_timed_out: boolean
}

For schema >= 1 (
  telemetry_level: 0 | 1 | 2
  
  For telemetry_level >= 1 (
    mod_version: string
    mod_platform: "fabric" | "forge" | "neoforge" | "quilt" | "unknown"
    mc_version: string

    # Automatically detected
    client_context: {
      country: {
        name: string
        code: string
      }
      timezone: string
    }
  )
)

For schema >= 2 (
  For telemetry_level >= 1 (
    current_hooks: {
      enchantment: string
      potion: string
    }
  )
  For telemetry_level >= 2 (
    all_hooks: {
      enchantment: string[]
      potion: string[]
    }
  )
)
